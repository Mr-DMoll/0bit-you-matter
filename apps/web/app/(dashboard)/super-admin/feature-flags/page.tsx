import { PlaceholderPage } from "@/shared/components/ui/PlaceholderPage";
export default function Page() {
  return <PlaceholderPage title="Feature Flags" description="Enable or disable platform features per environment without deploying code." />;
}
