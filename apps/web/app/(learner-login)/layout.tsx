export default function LearnerLoginLayout({ children }: { children: React.ReactNode }) {
  return (
    <div style={{ height: "100svh", overflow: "hidden", background: "#FAFAF9" }}>
      {children}
    </div>
  );
}
